import { Page, PageContainer } from '../components/PageLayout';

const Home = () => {
  return (
    <Page id="home">
      <PageContainer maxWidth="lg"></PageContainer>
    </Page>
  );
};

export default Home;
